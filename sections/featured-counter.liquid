{% assign sid = section.id | replace: '_', '' | downcase %}
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;1,300&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
.jw-counter-{{ sid }} {
  background: {{ section.settings.background_color }};
  padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
  border-top: 1px solid rgba(184,155,106,0.2);
  border-bottom: 1px solid rgba(184,155,106,0.2);
}
.jw-counter-inner-{{ sid }} {
  max-width: 1440px; margin: 0 auto; padding: 0 28px;
  display: grid; grid-template-columns: repeat(4,1fr); gap: 0;
}
.jw-ctr-item-{{ sid }} {
  text-align: center; padding: 36px 20px;
  border-right: 1px solid rgba(184,155,106,0.2);
  position: relative;
}
.jw-ctr-item-{{ sid }}:last-child { border-right: none; }
.jw-ctr-num-{{ sid }} {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(42px, 5vw, 68px);
  font-weight: 300; font-style: italic;
  line-height: 1; letter-spacing: 0.02em;
  margin-bottom: 8px;
}
.jw-ctr-label-{{ sid }} {
  font-family: 'Jost', sans-serif;
  font-size: 10.5px; font-weight: 600;
  letter-spacing: 0.16em; text-transform: uppercase;
  color: {{ section.settings.counter_label_color }};
}
.jw-ctr-line-{{ sid }} {
  width: 24px; height: 1px; background: #b89b6a;
  margin: 10px auto 0;
}
@media(max-width:768px) { .jw-counter-inner-{{ sid }} { grid-template-columns: repeat(2,1fr); } .jw-ctr-item-{{ sid }}:nth-child(2) { border-right: none; } .jw-ctr-item-{{ sid }}:nth-child(3),.jw-ctr-item-{{ sid }}:nth-child(4) { border-top: 1px solid rgba(184,155,106,0.2); } }
@media(max-width:480px) { .jw-counter-inner-{{ sid }} { grid-template-columns: 1fr; } .jw-ctr-item-{{ sid }} { border-right: none !important; border-top: 1px solid rgba(184,155,106,0.2) !important; } .jw-ctr-item-{{ sid }}:first-child { border-top: none !important; } }
</style>

<section class="jw-counter-{{ sid }}" {{ section.shopify_attributes }}>
  <div class="jw-counter-inner-{{ sid }}">
    {% for i in (1..4) %}
      {% assign num_key   = 'counter_' | append: i | append: '_number' %}
      {% assign plus_key  = 'counter_' | append: i | append: '_plus' %}
      {% assign label_key = 'counter_' | append: i | append: '_label' %}
      {% assign color_key = 'counter_' | append: i | append: '_number_color' %}
      {% if section.settings[label_key] != blank %}
        <div class="jw-ctr-item-{{ sid }}">
          <div class="jw-ctr-num-{{ sid }}" style="color:{{ section.settings[color_key] }}" data-target="{{ section.settings[num_key] }}" data-plus="{{ section.settings[plus_key] }}">0</div>
          <div class="jw-ctr-label-{{ sid }}">{{ section.settings[label_key] }}</div>
          <div class="jw-ctr-line-{{ sid }}"></div>
        </div>
      {% endif %}
    {% endfor %}
  </div>
</section>

<script>
(function() {
  var nums = document.querySelectorAll('#shopify-section-{{ section.id }} [data-target]');
  function animateCount(el) {
    var target = parseInt(el.dataset.target.replace(/,/g,''), 10);
    var plus = el.dataset.plus === 'true';
    var dur = 1800; var steps = 60; var step = 0;
    var t = setInterval(function() {
      step++;
      var val = Math.round(target * (step / steps));
      el.textContent = val.toLocaleString('en-IN') + (plus && step === steps ? '+' : '');
      if (step >= steps) { clearInterval(t); el.textContent = target.toLocaleString('en-IN') + (plus ? '+' : ''); }
    }, dur / steps);
  }
  var obs = new IntersectionObserver(function(entries) {
    entries.forEach(function(e) { if (e.isIntersecting) { animateCount(e.target); obs.unobserve(e.target); } });
  }, { threshold: 0.5 });
  nums.forEach(function(n) { obs.observe(n); });
})();
</script>

{% schema %}
{
  "name": "Stats Counter",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "counter_label_color",
      "label": "Label Color",
      "default": "#a8998a"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding Top",
      "min": 20,
      "max": 80,
      "step": 4,
      "default": 48,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding Bottom",
      "min": 20,
      "max": 80,
      "step": 4,
      "default": 48,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Counter 1"
    },
    {
      "type": "text",
      "id": "counter_1_number",
      "label": "Number",
      "default": "25000"
    },
    {
      "type": "checkbox",
      "id": "counter_1_plus",
      "label": "Show + sign",
      "default": true
    },
    {
      "type": "text",
      "id": "counter_1_label",
      "label": "Label",
      "default": "Happy Customers"
    },
    {
      "type": "color",
      "id": "counter_1_number_color",
      "label": "Number Color",
      "default": "#b89b6a"
    },
    {
      "type": "header",
      "content": "Counter 2"
    },
    {
      "type": "text",
      "id": "counter_2_number",
      "label": "Number",
      "default": "500"
    },
    {
      "type": "checkbox",
      "id": "counter_2_plus",
      "label": "Show + sign",
      "default": true
    },
    {
      "type": "text",
      "id": "counter_2_label",
      "label": "Label",
      "default": "Unique Designs"
    },
    {
      "type": "color",
      "id": "counter_2_number_color",
      "label": "Number Color",
      "default": "#d4b896"
    },
    {
      "type": "header",
      "content": "Counter 3"
    },
    {
      "type": "text",
      "id": "counter_3_number",
      "label": "Number",
      "default": "15"
    },
    {
      "type": "checkbox",
      "id": "counter_3_plus",
      "label": "Show + sign",
      "default": false
    },
    {
      "type": "text",
      "id": "counter_3_label",
      "label": "Label",
      "default": "Years of Craftsmanship"
    },
    {
      "type": "color",
      "id": "counter_3_number_color",
      "label": "Number Color",
      "default": "#b89b6a"
    },
    {
      "type": "header",
      "content": "Counter 4"
    },
    {
      "type": "text",
      "id": "counter_4_number",
      "label": "Number",
      "default": "100"
    },
    {
      "type": "checkbox",
      "id": "counter_4_plus",
      "label": "Show + sign",
      "default": false
    },
    {
      "type": "text",
      "id": "counter_4_label",
      "label": "Label",
      "default": "Certified Quality"
    },
    {
      "type": "color",
      "id": "counter_4_number_color",
      "label": "Number Color",
      "default": "#d4b896"
    }
  ],
  "presets": [
    {
      "name": "Stats Counter"
    }
  ]
}
{% endschema %}
